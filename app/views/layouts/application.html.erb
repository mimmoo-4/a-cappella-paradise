<!DOCTYPE html>
<html>
  <head>
    <title>あかぺらぱらだいす</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <script src="https://kit.fontawesome.com/f08898a6a5.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="navbar-logo p-3" href="/"><image_tag('logo.png') %></a>
          <button class="navbar-toggler" type="botton" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if member_signed_in? %>
                <li class="nav-item">
                  <%= link_to "新規投稿", new_post_path %>
                </li>
                <li class="nav-item">
                  <%= link_to "マイページ", member_path(current_member.id) %>
                </li>
                <li class="nav-item">
                  <%= link_to "ログアウト", destroy_member_session_path, method: :delete %>
                </li>
                <li class="nav-item">
                  <%= link_to "会員情報編集", edit_member_path(current_member.id) %>
                </li>
                <li class="nav-item">
                  <%= link_to "退会", member_path(current_member.id), method: :delete, data: { confirm: "本当に退会しますか？" } %>
                </li>
                <li class="nav-item">
                  <%= link_to "投稿一覧", posts_path %>
                </li>
                <li class="nav-item">
                  <%= link_to "アバウト", root_path %>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to "新規登録", new_member_registration_path %>
                </li>
                <li class="nav-item">
                  <%= link_to "ログイン", new_member_session_path %>
                </li>
                <li class="nav-item">
                  <%= link_to "アバウト", root_path %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= render 'public/searches/search' %>
    <%= yield %>
  </body>
</html>
