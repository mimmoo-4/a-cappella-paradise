<!DOCTYPE html>
<html>
  <head>
    <title>ACappellaParadise</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%=  javascript_pack_tag 'application', 'data-turbolinks-track': 'reload'  %>
  </head>

  <body>
    <header>
      <% if member_signed_in? %>
        <li>
          <%= link_to "新規投稿", new_post_path %>
        </li>
        <li>
          <%= link_to "マイページ", member_path(current_member.id) %>
        </li>
        <li>
          <%= link_to "ログアウト", destroy_member_session_path, method: :delete %>
        </li>
        <li>
          <%= link_to "会員情報編集", edit_member_path(current_member.id) %>
        </li>
        <li>
          <%= link_to "退会", member_path(current_member.id), method: :delete, data: { confirm: "本当に削除しますか？" } %>
        </li>
      <% else %>
        <li>
          <%= link_to "新規登録", new_member_registration_path %>
        </li>
        <li>
          <%= link_to "ログイン", new_member_session_path %>
        </li>
      <% end %>
    </header>
    <%= yield %>
  </body>
</html>
